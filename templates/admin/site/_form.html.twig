{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
    <div class="row">
        <div class="form-group col">
            {{ form_label(form.name, null, {'label_attr': {'class': ''}}) }}
            {{ form_widget(form.name, {'attr': {'class': "form-control"}}) }}
            <small class="form-text text-muted">* Obligatoire</small>
        </div>
        <div class="form-group col">
            {{ form_label(form.enterprise, null, {'label_attr': {'class': ''}}) }}
            {{ form_widget(form.enterprise, {'attr': {'class': "form-control"}}) }}
            {# <small class="form-text text-muted">* Obligatoire</small> #}
        </div>
    </div>
    <div class="row">
        <div class="form-group col">
            {{ form_label(form.latitude, null, {'label_attr': {'class': ''}}) }}
            {{ form_widget(form.latitude, {'attr': {'class': "form-control"}}) }}
            {# <small class="form-text text-muted">* Obligatoire</small> #}
        </div>
        <div class="form-group col">
            {{ form_label(form.longitude, null, {'label_attr': {'class': ''}}) }}
            {{ form_widget(form.longitude, {'attr': {'class': "form-control"}}) }}
            {# <small class="form-text text-muted">* Obligatoire</small> #}
        </div>
    </div>
    <div class="row">
        <div class="form-group col">
            {{ form_label(form.mainsInterruptDayLimit, null, {'label_attr': {'class': ''}}) }}
            {{ form_widget(form.mainsInterruptDayLimit, {'attr': {'class': "form-control"}}) }}
            {# <small class="form-text text-muted">* Obligatoire</small> #}
        </div>
        <div class="form-group col">
            {{ form_label(form.currency, null, {'label_attr': {'class': ''}}) }}
            {{ form_widget(form.currency, {'attr': {'class': "form-control"}}) }}
        </div>
    </div>
    <div class="row justify-content-center">
        <a href="#" id="getPos" class="btn btn-info waves-effect rounded" data-toggle="tooltip" data-placement="top" title="Get my current position ?"><i class="fas fa-crosshairs"></i></a>
        
    </div>  
    {% if isEdit|default(false) %}{{ form_widget(form.smartMods) }}{% endif %}
    
    {# <button class="btn">{{ button_label|default('Save') }}</button> #}
    <div class="row float-left">
        <a href="{{ path('admin_site_index') }}" class="btn btn-danger mr-2 mt-4 waves-effect">Back to list</a>
        <button type="submit" class="btn btn-primary mt-4 waves-effect" id="saveBtn">
            <span class="spinner-border spinner-border-sm mr-1 d-none" role="status" aria-hidden="true"></span>
            {{ button_label|default('Save') }}
        </button>
    
    </div>
{{ form_end(form) }}