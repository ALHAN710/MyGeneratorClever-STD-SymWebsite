<form method="post" action="{{ path('admin_smartMod_delete', {'id': smartMod.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ smartMod.id) }}">
    <button class="btn">Delete</button>
</form>
